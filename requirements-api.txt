# Slim runtime for the single-container deploy (read-only API + built dashboard, baked
# SQLite seed). CPU-only torch (the seed builds the GRU anomaly incidents at image-build
# time); no Postgres driver, no scikit-learn (the IF/PCA baselines are eval-only and import
# lazily), no Alembic (the SQLite seed needs no migration step).
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.115
uvicorn>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
structlog>=24.1
# httpx + tenacity: the demo seed imports the ingest module (AdsbSample lives there) at
# image-build time; the read-only API itself never fetches at runtime.
httpx>=0.27
tenacity>=8.3
numpy>=1.26
torch>=2.2
